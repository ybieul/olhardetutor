/**
 * Generated Supabase database types go here (e.g. via `supabase gen types
 * typescript`). Placeholder until the schema exists — every table added to
 * the project should be reflected in this type so client.ts stays typed.
 */
export type Database = Record<string, never>;
