import { PawPrint } from 'lucide-react';

import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export function OnboardingPage() {
  return <PlaceholderPage icon={PawPrint} titleKey="nav.onboarding" />;
}
