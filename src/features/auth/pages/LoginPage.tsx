import { LogIn } from 'lucide-react';

import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

export function LoginPage() {
  return <PlaceholderPage icon={LogIn} titleKey="nav.login" />;
}
